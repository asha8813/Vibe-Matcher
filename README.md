# 🧠 Intelligent Product Search using OpenAI Embeddings and FAISS

This project demonstrates how to build a **semantic product search system** using **OpenAI’s text embeddings** and **FAISS** for vector similarity search.  
Instead of relying on simple keyword matching, this system understands the **meaning** of product descriptions and finds the most relevant items for a given query.

---

## 🚀 Features

- Generates **vector embeddings** for product descriptions using OpenAI's `text-embedding-ada-002` model.  
- Uses **FAISS** (Facebook AI Similarity Search) for fast and efficient vector indexing.  
- Performs **semantic search** to retrieve products similar in meaning to a query.  
- Saves and reuses generated embeddings and FAISS index for future searches.

---

## 🧩 Technologies Used

- **Python 3.10+**  
- **OpenAI API**  
- **FAISS**  
- **NumPy**  
- **Pandas**

---

## 📦 Installation

1. **Clone this repository**

   ```bash
   git clone https://github.com/asha8813/Vibe-Matcher
  
